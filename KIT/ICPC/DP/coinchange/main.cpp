#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t, c;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        int64_t x;
        cin >> x >> c;
        int arr[c];
        for (int j = 0; j < c; j++)
        {
            cin >> arr[j];
        }
        
    }

    return 0;
}